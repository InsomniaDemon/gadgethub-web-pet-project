services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: backend
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      DB_HOST: postgres
      DB_PORT: 5432

  postgres:
    image: postgres:16
    container_name: postgres

    environment:
      POSTGRES_DB: gadgetHub
      POSTGRES_USER: gadgetHub
      POSTGRES_PASSWORD: gadgetHub

    ports:
      - "5433:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
