spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/gadgetHub
    username: ${DB_USER:gadgetHub}
    password: ${DB_PASSWORD:gadgetHub}